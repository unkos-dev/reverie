---
title: just recipes
description: Every task-runner recipe in the Reverie repository, generated from the justfiles.
---

import { Aside } from "@astrojs/starlight/components";

<Aside type="caution" title="Generated file">
  This page is generated from the justfiles by `just infra::just-reference`. Edit the recipe doc
  comments, not this page.
</Aside>

`just` is the task runner for every plane of the repository. Run `just` with no
arguments for the same list at a terminal, or `just --list` to include groups.

Recipes are namespaced by module: `just rust::check` runs the backend gate,
`just js::check` the frontend one. The unprefixed aggregates at the top fan out
across every plane.

`just preflight` and `just preflight-full` end with one machine-readable
verdict line, `GATE: PASS ...` or `GATE: FAIL ... at <lane>`, so a run read back
through a pipe, a truncated log, or a detached shell still says whether it
passed. `just gate-status` replays the last recorded run with its per-lane
timings, warns when the checkout has moved past the commit that run was
recorded on or picked up uncommitted changes it never saw, and exits nonzero
unless that run finished green: distinct statuses tell a failed run, a killed
one, one still in progress, and no recorded run at all apart. `just
preflight-detach` runs either gate detached from the terminal and prints the
log path to replay afterward.

| Recipe                                | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `just build`                          | Build every shippable artifact.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `just check`                          | Verify every plane (locally-runnable gates only; DB/CI recipes and a11y excluded).                                                                                                                                                                                                                                                                                                                                                                                                 |
| `just db-down`                        | Stop the local dev Postgres; the data volume survives.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `just db-migrate`                     | Apply pending migrations with the dedicated migrator identity.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `just db-migrate-raw <args>`          | Apply pending migrations directly with sqlx-cli, bypassing the backend build.                                                                                                                                                                                                                                                                                                                                                                                                      |
| `just db-reset`                       | Destroy and recreate the local dev Postgres, then re-seed roles. DESTRUCTIVE.                                                                                                                                                                                                                                                                                                                                                                                                      |
| `just db-up`                          | Start (or create) the local dev Postgres.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `just dev-down`                       | Stop the background dev servers (frontend, then backend).                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `just dev-status`                     | Report both dev servers' status; exits nonzero when either is down.                                                                                                                                                                                                                                                                                                                                                                                                                |
| `just dev-up`                         | Start the whole dev stack in the background: dev Postgres, migrations, backend API, Vite.                                                                                                                                                                                                                                                                                                                                                                                          |
| `just doctor`                         | Check the local dev environment: tools, mise pins, docker, dev DB, deps.                                                                                                                                                                                                                                                                                                                                                                                                           |
| `just fmt`                            | Format the whole tree in place. WRITES; never depended on by check/lint.                                                                                                                                                                                                                                                                                                                                                                                                           |
| `just gate-status`                    | Report the last recorded preflight run: per-lane timings and the verdict.                                                                                                                                                                                                                                                                                                                                                                                                          |
| `just help`                           | List every recipe, including the ones inside modules.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `just install`                        | Install the npm workspace (frontend + docs) from the root lockfile.                                                                                                                                                                                                                                                                                                                                                                                                                |
| `just lint`                           | Lint the js, rust, and infra surfaces.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `just preflight <args>`               | Run only the preflight lanes this branch's changed paths require (the default gate).                                                                                                                                                                                                                                                                                                                                                                                               |
| `just preflight-detach <mode> <args>` | Run the scoped (default) or full preflight gate detached from the terminal; see `just gate-status` for the verdict.                                                                                                                                                                                                                                                                                                                                                                |
| `just preflight-full`                 | Run everything CI runs that is locally runnable, DB-backed tests included, unconditionally.                                                                                                                                                                                                                                                                                                                                                                                        |
| `just test`                           | Run every locally-runnable unit test.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `just worktree <branch> <base>`       | Create a git worktree for BRANCH at `$WORKTREE_ROOT/reverie/<slug>`, with an isolated cargo target dir, the node dependencies installed under the npm the destination declares (so this recipe needs network access), and, when present, Claude and active Codex policy overlays carried over; a new branch bases on origin/main (falling back to local main, failing when neither exists) unless BASE is given explicitly; warns if Cargo environment overrides defeat isolation. |
| `just worktree-rm <branch>`           | Remove the worktree for BRANCH.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

## docs

| Recipe             | Purpose                                                                         |
| ------------------ | ------------------------------------------------------------------------------- |
| `just docs::build` | Build the Starlight site (= astro build).                                       |
| `just docs::check` | Locally-runnable docs gate: build only. Run `just docs::links` on demand after. |
| `just docs::links` | Internal-link check over the built site.                                        |

## infra

| Recipe                                           | Purpose                                                                                                                                          |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| `just infra::actionlint`                         | Lint GitHub Actions workflow files.                                                                                                              |
| `just infra::adr-frontmatter-selftest`           | Validate canonical ADR frontmatter and lifecycle index coverage.                                                                                 |
| `just infra::backend-dev-env-selftest`           | Assert the backend dev recipes resolve REVERIE_DEV_ENV without overriding an already-set variable.                                               |
| `just infra::cargo-locked-guard`                 | Every dependency-resolving cargo invocation in the just recipes must pass --locked; the package-ingress ADR records why.                         |
| `just infra::cargo-locked-guard-selftest`        | Self-test scripts/cargo-locked-guard.sh against clean and violating fixtures, then the real recipes.                                             |
| `just infra::check`                              | Infra has no formatter (oxfmt owns formatting via the JS plane), so check == lint.                                                               |
| `just infra::compose-dev-invariants`             | Dev-DB compose invariants: loopback bind, digest pin, parent volume mount.                                                                       |
| `just infra::compose-dev-invariants-selftest`    | Self-test both CI postgres pins against isolated workflow fixtures.                                                                              |
| `just infra::db-ready-selftest`                  | Self-test the db-up socket readiness probe against a stubbed psql.                                                                               |
| `just infra::dev-server-selftest`                | Exercise the dev-server lifecycle script against a fake server on a scratch port.                                                                |
| `just infra::doctor-selftest`                    | Self-test `just doctor` (missing-binary, fully-stubbed happy path, warn-only path).                                                              |
| `just infra::filter-sarif-test-rules-selftest`   | ordinary rule reported in a test file still reaches the dashboard.                                                                               |
| `just infra::filter-sarif-unfixable-os-selftest` | a fix available is never withheld.                                                                                                               |
| `just infra::gate-detach-selftest`               | Self-test the detached-gate script `just preflight-detach` wraps: a passing and a failing run each need a log and a gate-status-readable record. |
| `just infra::gate-run-selftest`                  | Self-test the gate runner's verdict line, run record, status reporting, and record pruning.                                                      |
| `just infra::greptile-config`                    | Validate greptile.json parses and every pinned context path resolves.                                                                            |
| `just infra::hadolint`                           | Hadolint every tracked Dockerfile.                                                                                                               |
| `just infra::just-reference`                     | Regenerate the just recipe reference from the justfiles. WRITES.                                                                                 |
| `just infra::just-reference-drift`               | Assert the committed just reference still matches the justfiles.                                                                                 |
| `just infra::lefthook-validate`                  | Validate the lefthook.yml hook configuration.                                                                                                    |
| `just infra::lint`                               | Whole repo-lint surface (mirrors the CI repo-lint job).                                                                                          |
| `just infra::no-issue-refs <files>`              | Reject issue-tracker references anywhere in the gated tree.                                                                                      |
| `just infra::no-issue-refs-selftest`             | Self-test the issue-tracker reference guard's whole-tree enforcement.                                                                            |
| `just infra::no-plan-refs <files>`               | Reject plan-artifact references in a passed file list.                                                                                           |
| `just infra::no-plan-refs-selftest`              | Self-test the private-plan reference guard.                                                                                                      |
| `just infra::npm-pin-drift`                      | Assert the npm pin agrees between package.json devEngines and mise.toml.                                                                         |
| `just infra::npm-pin-drift-selftest`             | Exercise the npm-pin guard against planted drift before trusting it.                                                                             |
| `just infra::preflight-scope-selftest`           | Self-test the preflight scoper's lane mapping, escalation, and fail-closed paths.                                                                |
| `just infra::prose`                              | Vale prose linter over all in-scope Markdown (docs/src, adr, repo-root).                                                                         |
| `just infra::recipe-secret-echo-test`            | Overridden DB credentials never echo into recipe logs (dry-run assertion).                                                                       |
| `just infra::renovate-npm-manager-selftest`      | Assert Renovate's custom manager still sees the devEngines npm pin.                                                                              |
| `just infra::require-disk-backed-selftest`       | Self-test the shared tmpfs/ramfs filesystem guard `just worktree` and `just doctor` both call.                                                   |
| `just infra::sanitize-sarif-severities-selftest` | Self-test the SARIF severity sanitizer both Snyk upload jobs run.                                                                                |
| `just infra::shellcheck`                         | Shellcheck every tracked shell script.                                                                                                           |
| `just infra::sqruff`                             | Lint every tracked SQL file against `.sqruff`.                                                                                                   |
| `just infra::tool-pin-drift`                     | Assert no workflow re-pins a mise-managed tool; mise.toml is the single source.                                                                  |
| `just infra::tool-pin-drift-selftest`            | Exercise the tool-pin guard against planted pins before trusting it.                                                                             |
| `just infra::typos`                              | Spell check the tree (honours .gitignore and \_typos.toml excludes).                                                                             |
| `just infra::vp-pin-drift`                       | Assert every vite-plus pin agrees: both package.json files, the alias spec, the override, and each workflow.                                     |
| `just infra::vp-pin-drift-selftest`              | Exercise the vp-pin guard against planted drift before trusting it.                                                                              |
| `just infra::worktree-base-selftest`             | Self-test the new-branch base resolver: origin/main, local-main and HEAD fallbacks, and the explicit-base override/rejection paths.              |
| `just infra::worktree-selftest`                  | Self-test the worktree recipe's Cargo isolation and operator-owned overlays.                                                                     |
| `just infra::yamllint`                           | yamllint every tracked YAML file.                                                                                                                |
| `just infra::zizmor`                             | Audit workflow files for GitHub Actions supply-chain and security issues (matches the CI zizmor gate).                                           |
| `just infra::zizmor-offline`                     | Offline-only workflow security audit; skips network-dependent audits (see just infra::zizmor for full CI parity).                                |

## js

| Recipe                           | Purpose                                                                               |
| -------------------------------- | ------------------------------------------------------------------------------------- |
| `just js::a11y`                  | WCAG axe-core scan of the design showcase; boots a browser and a dev server.          |
| `just js::build`                 | Frontend production build (vp build; typecheck rides js::oxlint via tsgo).            |
| `just js::check`                 | Locally-runnable verify set: lint + format check (a11y and coverage excluded).        |
| `just js::detect`                | Run the impeccable design anti-pattern detector over the frontend.                    |
| `just js::dev`                   | Run the Vite dev server in the foreground (Ctrl-C stops it).                          |
| `just js::dev-start`             | Start the Vite dev server in the background; logs to frontend/.dev-server.log.        |
| `just js::dev-status`            | Report whether the Vite dev server is up.                                             |
| `just js::dev-stop`              | Stop the background Vite dev server started by dev-start.                             |
| `just js::fmt`                   | Format the whole tree in place (TS/JS/JSON/CSS/MD/HTML/TOML/YAML). WRITES.            |
| `just js::fmt-check`             | Verify whole-tree formatting without writing (the authoritative CI gate form).        |
| `just js::font-integrity`        | Verify the committed Fontshare woff2 files against their pinned checksums.            |
| `just js::lint`                  | Frontend lint surface: oxlint, stylelint, then root-CWD markdownlint.                 |
| `just js::markdownlint <files>`  | markdownlint over tracked Markdown at the repo root.                                  |
| `just js::markdownlint-selftest` | Exercise YAML loading through the installed Markdownlint CLI before linting the tree. |
| `just js::oxlint <files>`        | oxlint over the frontend, or a passed file list (--max-warnings 0, matching CI).      |
| `just js::stylelint <files>`     | stylelint over frontend CSS (--max-warnings 0, matching CI).                          |
| `just js::test`                  | Frontend unit tests.                                                                  |
| `just js::test-coverage`         | Frontend unit tests with coverage (the CI form).                                      |

## rust

| Recipe                    | Purpose                                                                           |
| ------------------------- | --------------------------------------------------------------------------------- |
| `just rust::build`        | Compile the workspace.                                                            |
| `just rust::check`        | Locally-runnable backend gate: format check, clippy, drift.                       |
| `just rust::clippy`       | Clippy across the whole workspace, all targets, warnings denied.                  |
| `just rust::cov`          | DB: full test suite under coverage instrumentation.                               |
| `just rust::deny`         | Check advisories, license, bans, and source policy.                               |
| `just rust::dev`          | Run the backend API server in the foreground (Ctrl-C stops it).                   |
| `just rust::dev-start`    | Start the backend API server in the background; logs to backend/.dev-server.log.  |
| `just rust::dev-status`   | Report whether the backend API server is up.                                      |
| `just rust::dev-stop`     | Stop the background backend API server started by dev-start.                      |
| `just rust::doc-lint`     | Reject broken intra-doc links in the public docstrings.                           |
| `just rust::doctests`     | DB: doctests across the workspace.                                                |
| `just rust::drift`        | Generated-artifact drift guard (OpenAPI spec + config reference + config schema). |
| `just rust::fmt`          | Format the workspace in place. WRITES; never a dependency of check/lint.          |
| `just rust::fmt-check`    | Verify workspace formatting without writing (the pre-push gate form).             |
| `just rust::guards`       | Backend static allowlist and prose guards.                                        |
| `just rust::lint`         | Backend lint surface: clippy + doc-lint.                                          |
| `just rust::machete`      | Fail on unused workspace dependencies.                                            |
| `just rust::migrate`      | Apply pending database migrations against the resolved dev DSN, then exit.        |
| `just rust::regen`        | Regenerate the OpenAPI spec, config reference, and config schema in place.        |
| `just rust::sqlx-check`   | DB: sqlx query-cache freshness against the live schema.                           |
| `just rust::sqlx-prepare` | DB: regenerate the sqlx offline query cache from the live schema.                 |
| `just rust::test <args>`  | DB: full test suite, fast local loop (no coverage; see `cov`).                    |
