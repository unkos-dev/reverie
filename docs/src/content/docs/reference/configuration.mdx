---
title: Configuration
description: Environment variables that configure a Reverie instance.
---

import { Aside } from "@astrojs/starlight/components";

<Aside type="caution" title="Generated file">
This page is generated from the backend configuration schema. Do not edit it by
hand. Regenerate with `REGEN=1 cargo test --test gen_config_ref` and commit the
result. The drift test fails CI if it is stale.
</Aside>

Reverie is configured entirely through environment variables, read once at
startup. Secret-bearing variables are listed by name only; their values never
appear here. A required variable left unset refuses startup with a clear error.

| Variable | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `DATABASE_URL` | `string` | Yes |  | Primary database DSN (`DATABASE_URL`, required). Connections opened against this DSN run as `reverie_app`; user-facing queries acquire transactions through `crate::db::acquire_with_rls`. |
| `DATABASE_URL_MIGRATION` | `string` | Conditional |  | Migration DSN (`DATABASE_URL_MIGRATION`). `reverie_migrator` credentials for the ephemeral migration pool. `None` on the default server path: the application process holds no migration credential unless `Self::auto_migrate` is set. Required (else `ConfigError::MissingVar`) only when `auto_migrate` is true. |
| `DATABASE_URL_INGESTION` | `string` | No |  | Ingestion-pipeline DSN (`DATABASE_URL_INGESTION`); falls back to `database_url` when unset. Connections run as `reverie_ingestion` against the `*_ingestion_full_access` RLS policies. |
| `OIDC_ISSUER_URL` | `string` | Conditional |  | OIDC issuer URL (`OIDC_ISSUER_URL`): the trust seam for the OIDC authentication path. OIDC is enabled iff this is set (decision 11); when present, the other three `OIDC_*` fields become required together (Gate 4). The boundary control is `reqwest`'s TLS validation against the bundled webpki/Mozilla root store (`reqwest` is built with the `rustls` feature, which uses `webpki-roots`, not OS system roots). |
| `OIDC_CLIENT_ID` | `string` | Conditional |  | OIDC client id (`OIDC_CLIENT_ID`, required). |
| `OIDC_CLIENT_SECRET` | `string` | Conditional |  | OIDC client secret (`OIDC_CLIENT_SECRET`, required). Treated as secret material; never logged. NOTE: any new secret-bearing field must also be added to the `SECRET_FIELDS` list so a deserialize error never echoes its value (hard rule 7). |
| `OIDC_REDIRECT_URI` | `string` | Conditional |  | OIDC redirect URI (`OIDC_REDIRECT_URI`). Required when OIDC is configured (Gate 4); must match the value registered with the issuer. |
| `REVERIE_LOCAL_AUTH_ENABLED` | `boolean` | No | `true` | Whether local email+password authentication is enabled (`REVERIE_LOCAL_AUTH_ENABLED`, default `true`). Reverie is local-first out of the box (ADR `2026-06-23-auth-identity-pluggable-providers`), so this defaults on. Setting it `false` without configuring OIDC is rejected at startup (Gate 4): at least one auth provider must remain usable or the instance locks everyone out. |
| `REVERIE_PORT` | `integer` | No | `3000` | HTTP listen port (`REVERIE_PORT`, default `3000`). |
| `REVERIE_LIBRARY_PATH` | `string` | No | `./library` | Filesystem root for persisted manifestation files (`REVERIE_LIBRARY_PATH`, default `./library`). The OPDS download handler canonicalises file paths against this root. |
| `REVERIE_INGESTION_PATH` | `string` | No | `./ingestion` | Watched ingestion drop directory (`REVERIE_INGESTION_PATH`, default `./ingestion`). The watcher consumes files from here. |
| `REVERIE_QUARANTINE_PATH` | `string` | No | `./quarantine` | Failed-ingestion quarantine directory (`REVERIE_QUARANTINE_PATH`, default `./quarantine`). |
| `REVERIE_LOG_LEVEL` | `string` | No | `info` | Log-filter directive resolved from the environment with cascading precedence: `REVERIE_LOG_LEVEL` > `RUST_LOG` > `"info"`. The `REVERIE_*` operator namespace wins on conflict so staging docs stay coherent; `RUST_LOG` is honoured as the ecosystem default for developer convenience. The subscriber filter in `crate::run` parses this string directly (no further env re-read), so the precedence resolved here is the single source of truth for the process lifetime. |
| `RUST_LOG` | `string` | No | `info` | Log-filter directive resolved from the environment with cascading precedence: `REVERIE_LOG_LEVEL` > `RUST_LOG` > `"info"`. The `REVERIE_*` operator namespace wins on conflict so staging docs stay coherent; `RUST_LOG` is honoured as the ecosystem default for developer convenience. The subscriber filter in `crate::run` parses this string directly (no further env re-read), so the precedence resolved here is the single source of truth for the process lifetime. |
| `REVERIE_DB_MAX_CONNECTIONS` | `integer` | No | `10` | Per-pool connection cap (`REVERIE_DB_MAX_CONNECTIONS`, default `10`); applied identically to the primary, ingestion, and writeback pools. Must be ≥ 1: a zero cap yields a pool that can never hand out a connection (`PoolTimedOut` on the first query). |
| `REVERIE_LOGIN_RATE_PER_MIN` | `integer` | No | `10` | Per-source login rate limit, attempts per minute per client IP (`REVERIE_LOGIN_RATE_PER_MIN`, default `10`). The hard blocker against credential-stuffing; must be at least 1 (a zero quota locks everyone out). |
| `REVERIE_LOGIN_THROTTLE_BASE_SECS` | `integer` | No | `2` | Per-account backoff base, seconds (`REVERIE_LOGIN_THROTTLE_BASE_SECS`, default `2`). The first failed login for an account waits the lesser of base and cap; each further failure doubles the base up to the cap. |
| `REVERIE_LOGIN_THROTTLE_CAP_SECS` | `integer` | No | `900` | Per-account backoff cap, seconds (`REVERIE_LOGIN_THROTTLE_CAP_SECS`, default `900`). Upper bound on the escalating per-account delay. |
| `REVERIE_PASSWORD_MIN_LENGTH` | `integer` | No | `8` | Minimum length for a local-account password (`REVERIE_PASSWORD_MIN_LENGTH`, default `8`, the NIST SP 800-63B basic floor). Enforced at first-run setup and password reset. S2 ships only the length floor; breach (HIBP) and strength (zxcvbn) checks are S3. |
| `REVERIE_RECOVERY_PIN_TTL_SECS` | `integer` | No | `900` | Lifetime of a forgot-password recovery PIN, seconds (`REVERIE_RECOVERY_PIN_TTL_SECS`, default `900` = 15 minutes). Short by design: the PIN is single-use and rate-limited. |
| `REVERIE_RECOVERY_PIN_FILE_PATH` | `string` | No | `./reverie-recovery.pin` | Filesystem path the clear recovery PIN is written to (mode 0600), for an operator to read and relay (`REVERIE_RECOVERY_PIN_FILE_PATH`, default `./reverie-recovery.pin`). MUST be outside any web-served directory; the database stores only the PIN's Argon2id hash. |
| `REVERIE_TRUSTED_CLIENT_IP_HEADER` | `string` | No |  | Optional forwarded-for header to trust for the client IP behind a reverse proxy (`REVERIE_TRUSTED_CLIENT_IP_HEADER`, e.g. `X-Forwarded-For`). Unset by default: the TCP peer is used. An unauthenticated forwarded header is attacker-spoofable, so it is honoured only when an operator names it. |
| `REVERIE_AUTO_MIGRATE` | `boolean` | No | `false` | Run pending migrations in-process at startup (`REVERIE_AUTO_MIGRATE`, default `false`). The shipped default is out-of-band migration via `reverie migrate`; when this is `true` the long-lived server process carries the migration credential for its whole lifetime, so it is an opt-in escape hatch only. Requires `Self::migration_database_url` to be set. |
| `REVERIE_FORMAT_PRIORITY` | list of `epub` \| `pdf` \| `mobi` \| `azw3` \| `cbz` \| `cbr` | No | `epub, pdf, mobi, azw3, cbz, cbr` | Ranked acceptable formats (`REVERIE_FORMAT_PRIORITY`, comma-separated; default `epub,pdf,mobi,azw3,cbz,cbr`). The ingestion pipeline picks the highest-ranked file when an incoming work has multiple candidates. |
| `REVERIE_CLEANUP_MODE` | `all` \| `ingested` \| `none` | No | `all` | Post-ingestion cleanup behaviour (`REVERIE_CLEANUP_MODE`, default `all`). See `CleanupMode` for variant semantics. |
| `REVERIE_OPENLIBRARY_BASE_URL` | `string` | No | `https://openlibrary.org` | `OpenLibrary` API base URL (`REVERIE_OPENLIBRARY_BASE_URL`, default `https://openlibrary.org`). |
| `REVERIE_GOOGLEBOOKS_BASE_URL` | `string` | No | `https://www.googleapis.com/books/v1` | Google Books API base URL (`REVERIE_GOOGLEBOOKS_BASE_URL`, default `https://www.googleapis.com/books/v1`). |
| `REVERIE_GOOGLEBOOKS_API_KEY` | `string` | No |  | Optional Google Books API key (`REVERIE_GOOGLEBOOKS_API_KEY`); when set, requests bypass the public anonymous quota. |
| `REVERIE_HARDCOVER_BASE_URL` | `string` | No | `https://api.hardcover.app/v1/graphql` | Hardcover GraphQL endpoint (`REVERIE_HARDCOVER_BASE_URL`, default `https://api.hardcover.app/v1/graphql`). |
| `REVERIE_HARDCOVER_API_TOKEN` | `string` | No |  | Optional Hardcover bearer token (`REVERIE_HARDCOVER_API_TOKEN`); requests are skipped when unset. |
| `REVERIE_OPERATOR_CONTACT` | `string` | No |  | Operator contact (`REVERIE_OPERATOR_CONTACT`); embedded into the outbound `User-Agent` to claim `OpenLibrary`'s identified 3 req/s rate-limit tier (vs. 1 req/s anonymous). |
| `REVERIE_ENRICHMENT_ENABLED` | `boolean` | No | `true` | Whether the enrichment queue is spawned (`REVERIE_ENRICHMENT_ENABLED`, default `true`). |
| `REVERIE_ENRICHMENT_CONCURRENCY` | `integer` | No | `2` | In-flight enrichment job concurrency (`REVERIE_ENRICHMENT_CONCURRENCY`, default `2`; valid range 1-10). |
| `REVERIE_ENRICHMENT_POLL_IDLE_SECS` | `integer` | No | `30` | Sleep between empty-queue polls (`REVERIE_ENRICHMENT_POLL_IDLE_SECS`, default `30`). |
| `REVERIE_ENRICHMENT_FETCH_BUDGET_SECS` | `integer` | No | `15` | Per-job overall fetch budget (`REVERIE_ENRICHMENT_FETCH_BUDGET_SECS`, default `15`). |
| `REVERIE_ENRICHMENT_HTTP_TIMEOUT_SECS` | `integer` | No | `10` | Per-request HTTP timeout for outbound metadata fetches (`REVERIE_ENRICHMENT_HTTP_TIMEOUT_SECS`, default `10`). |
| `REVERIE_ENRICHMENT_MAX_ATTEMPTS` | `integer` | No | `10` | Maximum retry attempts before a job is considered exhausted (`REVERIE_ENRICHMENT_MAX_ATTEMPTS`, default `10`). |
| `REVERIE_ENRICHMENT_CACHE_TTL_HIT_DAYS` | `integer` | No | `30` | Cache TTL for successful (`hit`) responses (`REVERIE_ENRICHMENT_CACHE_TTL_HIT_DAYS`, default `30`). |
| `REVERIE_ENRICHMENT_CACHE_TTL_MISS_DAYS` | `integer` | No | `7` | Cache TTL for "not found" (`miss`) responses (`REVERIE_ENRICHMENT_CACHE_TTL_MISS_DAYS`, default `7`). |
| `REVERIE_ENRICHMENT_CACHE_TTL_ERROR_MINS` | `integer` | No | `15` | Cache TTL for transient-error responses (`REVERIE_ENRICHMENT_CACHE_TTL_ERROR_MINS`, default `15`). |
| `REVERIE_COVER_MAX_BYTES` | `integer` | No | `10485760` | Maximum bytes accepted per cover image (`REVERIE_COVER_MAX_BYTES`, default `10_485_760`, 10 MiB). Must be ≥ 1: a zero cap would reject every cover. |
| `REVERIE_COVER_DOWNLOAD_TIMEOUT_SECS` | `integer` | No | `30` | Per-download HTTP timeout (`REVERIE_COVER_DOWNLOAD_TIMEOUT_SECS`, default `30`). Must be ≥ 1: a zero timeout would abort every download immediately. |
| `REVERIE_COVER_MIN_LONG_EDGE_PX` | `integer` | No | `1000` | Minimum long-edge pixel dimension; smaller images are rejected (`REVERIE_COVER_MIN_LONG_EDGE_PX`, default `1000`). Must be ≥ 1: a zero floor would defeat the quality filter and accept any image. |
| `REVERIE_COVER_REDIRECT_LIMIT` | `integer` | No | `3` | Maximum HTTP redirect hops the cover fetcher will follow (`REVERIE_COVER_REDIRECT_LIMIT`, default `3`). |
| `REVERIE_WRITEBACK_ENABLED` | `boolean` | No | `true` | Whether the writeback worker is spawned (`REVERIE_WRITEBACK_ENABLED`, default `true`). |
| `REVERIE_WRITEBACK_CONCURRENCY` | `integer` | No | `2` | In-flight writeback job concurrency (`REVERIE_WRITEBACK_CONCURRENCY`, default `2`; valid range 1-10). |
| `REVERIE_WRITEBACK_POLL_IDLE_SECS` | `integer` | No | `5` | Sleep between empty-queue polls (`REVERIE_WRITEBACK_POLL_IDLE_SECS`, default `5`). |
| `REVERIE_WRITEBACK_MAX_ATTEMPTS` | `integer` | No | `10` | Maximum retry attempts before a writeback job is considered exhausted (`REVERIE_WRITEBACK_MAX_ATTEMPTS`, default `10`). |
| `REVERIE_OPDS_ENABLED` | `boolean` | No | `true` | Whether the `/opds/*` routes are mounted (`REVERIE_OPDS_ENABLED`, default `true`). |
| `REVERIE_OPDS_PAGE_SIZE` | `integer` | No | `50` | Default page size for paginated feeds (`REVERIE_OPDS_PAGE_SIZE`, default `50`; valid range 1-500). |
| `REVERIE_OPDS_REALM` | `string` | No | `Reverie OPDS` | `WWW-Authenticate: Basic realm=...` value emitted on 401 responses from `/opds/*` (`REVERIE_OPDS_REALM`, default `"Reverie OPDS"`). Validated to exclude `"` to keep the header well-formed. |
| `REVERIE_PUBLIC_URL` | `URL` | No |  | Externally-visible base URL the catalogue emits absolute links rooted at (`REVERIE_PUBLIC_URL`). Required when `enabled=true`; optional otherwise. |
| `REVERIE_BEHIND_HTTPS` | `boolean` | No | `false` | Whether the deployment is fronted by a TLS-terminating reverse proxy (`REVERIE_BEHIND_HTTPS`, default `false`). Gates HSTS emission: never emitted on plaintext HTTP because the browser would refuse the next TLS-less request to this host. |
| `REVERIE_HSTS_INCLUDE_SUBDOMAINS` | `boolean` | No | `false` | Whether the HSTS header carries `; includeSubDomains` (`REVERIE_HSTS_INCLUDE_SUBDOMAINS`, default `false`). Requires `behind_https=true`. |
| `REVERIE_HSTS_PRELOAD` | `boolean` | No | `false` | Whether the HSTS header carries `; preload` (`REVERIE_HSTS_PRELOAD`, default `false`). Requires `hsts_include_subdomains=true` (chrome.com / hstspreload.org rules). |
| `REVERIE_CSP_REPORT_ENDPOINT` | `URL` | No |  | Optional CSP-violation reporting endpoint (`REVERIE_CSP_REPORT_ENDPOINT`). Pre-validated at startup to reject `"`/`;`/CR/LF (header-injection guard) and any scheme other than `http`/`https`. |
| `REVERIE_FRONTEND_DIST_PATH` | `string` | No |  | Optional path to the built frontend dist directory (`REVERIE_FRONTEND_DIST_PATH`). When set, the SPA assets router is mounted and the FOUC-script hash is read at startup to seed the HTML CSP. |
