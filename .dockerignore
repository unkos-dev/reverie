.git
.github
.gitignore
.vscode
.idea
# docs is excluded wholesale, but its manifest must reach the build context:
# npm needs every workspace's package.json to resolve the workspace graph
# (the frontend-builder stage installs only the frontend workspace's deps).
docs
!docs/package.json
target
node_modules
dist
*.md
!README.md
LICENSE
