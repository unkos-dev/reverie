# Normalize all text to LF in the repo and on checkout (incl. Windows),
# so prettier's endOfLine:lf and CRLF-on-checkout never fight.
* text=auto eol=lf

# Binary assets — never touch line endings or diff as text.
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
*.ico   binary
*.woff  binary
*.woff2 binary
*.ttf   binary
*.otf   binary
*.pdf   binary
*.epub  binary
*.zip   binary
