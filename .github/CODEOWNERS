# Scoped to .github/workflows/** so workflow changes require code-owner
# review: a privileged self-hosted runner executes these files as root, and
# this entry adds a review-surface signal alongside the runner-side deploy
# gate so a workflow change can't land unnoticed inside a larger diff.

/.github/workflows/         @junkovich
