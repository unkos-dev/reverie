target/
node_modules/
dist/
docs/.astro/
CHANGELOG.md
backend/.sqlx/

# Vendored shadcn/ui components — generated by `npx shadcn add`, formatted in
# shadcn's upstream style. Kept pristine so re-syncs / future `add`s produce
# clean diffs instead of fighting our prettier config on every regeneration.
frontend/src/components/ui/

# Private agent workspace (gitignored symlink into dotfiles) + relocated
# codeguard security context — frozen as authored; not worth format churn.
.claude/
docs/security/codeguard/

# axe accessibility-gate report artifact (generated by `npm run a11y`)
frontend/a11y-results.json

# Generated config JSON Schema (emitted by `reverie-api print-config-schema`,
# CI-drift-checked against that exact output). Prettier would reflow it and
# break the byte-for-byte drift gate; keep it as schemars emits it.
backend/config.schema.json

# Generated config reference page (rendered from the schema by the
# config-reference renderer, drift-gated by tests/gen_config_ref.rs). Same
# rationale as the schema above: prettier would reflow the table and break the
# byte-for-byte drift gate.
docs/src/content/docs/reference/configuration.mdx

# Generated OpenAPI spec (emitted code-first from the handlers, drift-gated by
# tests/gen_openapi.rs). Same rationale: prettier would reflow the JSON and
# break the byte-for-byte drift gate; keep it as utoipa emits it.
docs/openapi.json
